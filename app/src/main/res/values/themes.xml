<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.OnList" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="background">@color/green_onlist</item>
        <item name="colorPrimaryDark">@color/green_onlist_2</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="android:statusBarColor">@color/green_onlist</item>
        <!-- Customize your theme here. -->
    </style>
</resources>